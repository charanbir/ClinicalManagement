<%= form_for(@user_study) do |f| %>
  <% if @user_study.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_study.errors.count, "error") %> prohibited this user_study from being saved:</h2>

      <ul>
      <% @user_study.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    User ID:
  <%= f.text_field :User_id, :value => current_user.id, :readonly => true %>
  </div>
  <div class="field">
    Unique Study ID:
    <%= f.text_field :Study_id %>
  </div>
    <div class="field">
    Gender:
    <%= f.text_field :gender %>
  </div>
    <div class="field">
    Date of Birth:
    <%= f.text_field :dob %>
  </div>
    <div class="field">
    Address (Optional) :
    <%= f.text_field :address %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
